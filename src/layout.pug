doctype html
html.no-js(lang='en')
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
    title
    meta(name="viewport", content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no')
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js")
    link(href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css", rel="stylesheet", integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T", crossorigin="anonymous")
    script(src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js", integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM", crossorigin="anonymous")
    script(type='text/javascript').

      $(document).ready(function(){
        $(".btn-link").on( "click", function() {
          var btnLink = $(this);
          $('iframe').each(function() {
            var sizeIframe = $(this).attr('data-size');
            var getIframe = btnLink.attr('data-target');
            var heightIframe = sizeIframe.split('x');
            $(this).height(heightIframe[1]);
            sizeIframe = sizeIframe.substr(sizeIframe.length - 6);
            getIframe = getIframe.substr(getIframe.length - 6);
            if(getIframe == sizeIframe){
              this.contentWindow.location.reload(true);
            }
          });
        });
      });
    style.
      body {
        background-color: #f6f6f6;
      }
      iframe {
        display: block;
        margin: 0 auto;
        border: 0px #ffffff none;
        max-width:100%;
      }
      h2 {
        font-family: sans-serif;
        font-size: 26px;
        font-weight: 600;
      }
      a {
        color: #000!important;
        font-size: 15px;
      }
      table>tbody>tr>td {
        padding: 27px 3px!important;
        border-top: 1px solid #818181 !important;
        text-align: right;
      }
      .main {
        background-color: #fff;
        margin-top: 5%;
      }
      .banner-client {
        padding-top: 100px;
      }
      .tablist {
        padding-bottom:10px;
      }
      .banner-name {
        font-family: sans-serif;
        border-top: 1px solid #fff;
        font-weight: 300;
        margin-top: 46px;
        margin-bottom: 50px;
      }
      .banner-size {
        font-family: sans-serif;
        font-size: 15px;
        text-transform: capitalize;
      }
      .zip-size {
        font-family: sans-serif;
        font-size: 15px;
        float: right;
        color: #007bff;
        text-transform: uppercase;
        font-weight: bold;
      }
      .disclaimer {
        text-align: center;
         padding-top: 20px;
      }
      .main-container {
        padding-bottom: 100px;
      }

  body
    block content
  script(type='text/javascript').
    $(document).ready(function(){
      var i = 0;
      $(".isi-toggle").on("click", function() {
        $(this).parent('.card-body').toggleClass('showISI');
        if($('.card-body').hasClass('showISI')){
          $(this).html('Hide ISI');
          var iframeHeight = $('.showISI iframe').contents().find('.isi').height();
          $('.showISI iframe').height(iframeHeight + 180);
          $('.showISI iframe').contents().find('#wrapper, .isi_wrapper, .iframePreview').height(iframeHeight + 170);
        }else{
          $('iframe').each(function() {
            var sizeIframe = $(this).attr('data-size');
            sizeIframe = sizeIframe.split('x');
            $(this).height(sizeIframe[1]);
            setTimeout(() => { 
              this.contentWindow.myScroll.refresh();
            }, 500);
          });
          $(this).html('Show ISI');
          $('iframe').contents().find('#wrapper, .iframePreview, .isi_wrapper').removeAttr('style');
        }
      });
    });